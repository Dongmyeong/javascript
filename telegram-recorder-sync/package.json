{
  "name": "telegram-recorder-sync",
  "version": "1.0.0",
  "description": "Watch a folder for new recording files and automatically send them to Telegram via the Bot API. Designed to run on Android (Termux) with zero external dependencies.",
  "main": "index.js",
  "bin": {
    "telegram-recorder-sync": "index.js"
  },
  "scripts": {
    "start": "node index.js",
    "check": "node --check index.js && node --check src/config.js && node --check src/telegram.js && node --check src/state.js && node --check src/watcher.js && node --check src/logger.js",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "private": true
}
