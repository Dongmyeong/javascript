{
  "name": "mac-receiver-reassemble",
  "version": "1.0.0",
  "description": "Watch the inbound folder and stitch split part files (name.m4a.partNofM) back into the original recording, for the Mac mini transcription pipeline.",
  "main": "receive-telegram.js",
  "bin": {
    "receive-telegram": "receive-telegram.js",
    "reassemble-parts": "reassemble-parts.js"
  },
  "scripts": {
    "start": "node receive-telegram.js",
    "check": "for f in receive-telegram.js reassemble-parts.js lib/*.js test/*.js; do node --check \"$f\" || exit 1; done",
    "test": "node --test"
  },
  "engines": {
    "node": ">=18"
  },
  "license": "MIT",
  "private": true
}
